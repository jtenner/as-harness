# Changelog

## 2026-03-21

- **source-hosts: widen Windows temp cleanup retries for spawned CLI smoke runs** Keep the retry-based cleanup hardening for native host smoke suites, but widen the Windows retry window to cover the longer handle-release lag still seen on GitHub-hosted Node 25 runners after successful `wazero` subprocess execution; keep the repo-controlled packaged `wazero` addon loading path and the dedicated March 2026 CI flakiness log in place. GitHub: *@jtenner*
- **source-hosts: make Windows temp cleanup retries repo-controlled** Replace the native host smoke suites' dependence on Node's internal `rmSync` retry behavior with a shared repo-owned synchronous retry loop for `EPERM`/`EBUSY`/`ENOTEMPTY` temp-tree cleanup races, add direct unit coverage for that retry helper, and keep the CI flakiness log aligned with the latest Windows source-host failure mode. GitHub: *@jtenner*
- **wazero: load the source CLI host from the native addon directly** Stop the CLI's source `wazero` runtime from routing through `harness/wazero/index.cjs` under Bun on Windows, load `harness/wazero/dist/wazero.node` directly instead, decorate the harness inside `cli/runtime/wazero.ts`, and export a top-level `createHarness(...)` worker entry so the source and bundled CLI paths share the same runtime contract. GitHub: *@jtenner*
- **wazero: load extracted packaged addons through normal require resolution** Keep the bundled packaged `wazero` payload extraction path, but stop calling `process.dlopen(...)` with a synthetic module record and instead load the extracted absolute `.node` path through normal `require(...)` resolution so hosted Linux packaged runs can exit cleanly after successful `wazero` smoke execution. GitHub: *@jtenner*
- **packaging: run packaged smoke under a real clean environment** Stop the packaged verifier from inheriting the full CI toolchain environment during staged smoke runs, keep only the small cross-platform runtime env surface the packaged executable actually needs, and add direct verifier coverage so the “clean-environment staged executable” contract stays true. GitHub: *@jtenner*
- **wazero: restore bundled packaged close teardown** Remove the old packaged-CLI `wazero` close no-op so bundled Linux `wazero` commands tear down their native harnesses again and stop hanging after successful execution, while keeping the current interpreter-engine workaround in place. GitHub: *@jtenner*
- **tooling: pin CI and local toolchains in mise** Move the CI and release workflows onto `jdx/mise-action@v4`, make repo-local [`.mise.toml`](./.mise.toml) the shared source of truth for Bun `1.3.11`, Node `25.8.1`, Go `1.26.1`, and Rust `1.94.0`, keep the source-host matrix on the Node 25 baseline, and refresh the release docs, root README, and live backlog to match the new toolchain contract. GitHub: *@jtenner*
- **source-hosts: stop shared smoke compilation from shelling through npx.cmd** Switch the shared smoke fixture compiler from `npx` command shims to the repo-local `assemblyscript/bin/asc.js` entrypoint under `process.execPath`, add direct coverage for that command resolution, and restore the Windows source-host matrix path that was failing with `spawnSync npx.cmd EINVAL`. GitHub: *@jtenner*
- **packaging: keep bundled wazero inside archived release executables** Switch packaged release assets from renamed flat binaries to target-specific archives that preserve the inner executable basename, prove that bundled Linux `wazero` still passes from the clean staged install path without any `.node` sidecar, add direct archive-name coverage, and refresh the release docs plus live backlog to reflect the Bun standalone basename constraint. GitHub: *@jtenner*
- **ci: give packaged builds a longer verifier timeout budget** Keep packaged smoke supervision at 60 seconds so bundled host hangs still fail quickly, but give packaged build steps a separate 180-second budget so first-time native addon builds on hosted macOS x64 do not fail as false smoke-equivalent timeouts; update the packaged verifier proof, release/script docs, and live backlog to match. GitHub: *@jtenner*
- **wazero: force bundled Linux packaged runs onto the interpreter engine** Switch the bundled Linux `wazero` createHarness path onto wazero's interpreter engine, add native trace checkpoints plus a Go unit test for the internal engine switch, make the shared smoke fixture compiler use `npx.cmd` on Windows runners, and update the live backlog and host notes around the remaining compiler-engine restoration risk. GitHub: *@jtenner*
- **ci: point Go cache setup at the real wazero module and drop secondary report-upload noise** Tell `actions/setup-go` to cache against `harness/wazero/go.mod` instead of the repo root, switch packaged-report artifact uploads to ignore missing files when the verifier dies early, and record the still-open bundled Linux `wazero` hang in the live backlog. GitHub: *@jtenner*
- **ci: pin native workflow toolchains to current Go and Rust stable releases** Move CI and release native-host setup onto Go `1.26.1` and Rust `1.94.0`, document those baselines in the release process and native-host READMEs, and note the remaining upstream-toolchain rollover risk in the live backlog so packaged verification keeps matching the repo's native host requirements. GitHub: *@jtenner*
- **ci: stop assuming a prebuilt source `wazero` addon in root CLI tests** Keep the shipped `wazero` runtime on its normal worker-slot path, make the root Bun CLI coverage include `wazero` only when the local source addon has actually been built, and move CI and release workflows onto Node 24-compatible GitHub Action majors plus Bun `1.3.11` to match the stable packaged verification environment. GitHub: *@jtenner*
- **docs: retire the stale graph-aware scheduling blocker list** Clear the remaining shipped graph-scheduler bullets from `agent-todo.md`, mark the Vitest scheduling strategy note as historical guidance for the now-landed `v0.3.0` planner slice, and keep the live backlog focused only on real unreleased work. GitHub: *@jtenner*
- **proof: add direct planner coverage for only-filtered prerequisites** Extend the pure shared planner tests so `only`-filtered prerequisite declarations now lock into the same missing-dependency behavior already proved for `skip` and `todo`, refresh the host-runner proof note, and retire the completed pure-planner checklist item from the live backlog. GitHub: *@jtenner*
- **contract: require detached `start()` metadata snapshots** Tighten `HarnessStartResult` so shipped hosts always return a required `metadata` snapshot, clone the shared summary fields so `metadata` no longer aliases the top-level arrays, remove obsolete reporter fallback logic, prove the new contract through shared smoke coverage, and retire the completed host-contract backlog item. GitHub: *@jtenner*
- **contract: add concise issue labels to shared planning results** Extend `HarnessPlanIssue` and `HarnessBlockedNode` with shipped `issueLabel` fields, populate them from the shared scheduler, prove the new result shape through shared smoke and reporter coverage, and remove the completed backlog item for blocked-result semantics. GitHub: *@jtenner*
- **reporter: tighten blocked-outcome CLI copy** Replace raw planner issue codes in the CLI reporter with concise user-facing phrases for blocked prerequisites, missing prerequisites, and dependency cycles; add reporter and CLI proof for the new wording; and remove the completed backlog item. GitHub: *@jtenner*
- **assembly: add the first native `as-harness` guest adapter slice** Ship a bundled `"as-harness"` library with chainable declaration handles, sequential-group metadata, shared hook/context exports, and end-to-end proof through AssemblyScript internals, compiler bundling, and CLI host runs; then trim the completed native-adapter backlog items and refresh the user-facing docs. GitHub: *@jtenner*
- **proof: add direct duplicate-edge planner coverage** Add a shared planner regression test that proves mixed dependency metadata collapses to one execution edge, retire the stale duplicate-edge blocker from `agent-todo.md`, and align the graph-planning strategy note with the concrete unit coverage. GitHub: *@jtenner*

## 2026-03-20

- **proof: add direct planner edge-case coverage for filtered prerequisites and ancestor node IDs** Extend the shared planner unit tests to cover `todo` prerequisites as missing dependencies and dependency-node-id resolution through ancestor scopes, then remove the stale planner coverage risk from the live backlog. GitHub: *@jtenner*
- **proof: close the worker-slot scheduling parity risk with a larger ready-stage proof** Add a shared parallel-ready fixture that stages four ready branches, run the shared planner suite in parallel mode through `js`, `wazero`, and `wasmtime`, and remove the resolved worker-slot parity risk from the live backlog. GitHub: *@jtenner*
- **docs: refresh the live risk list for worker-slot scheduling parity** Remove the obsolete note that `start()` keeps worker-aware parallelism out of scope, replace it with the current cross-host parity risk for shared worker-slot scheduling, and keep the remaining risk list aligned with the shipped scheduler. GitHub: *@jtenner*
- **docs: align the host READMEs with the planner-shaped scheduler report** Update the package READMEs and CLI report notes so `start()` is described as deterministic planning plus same-machine worker slots and the CLI report docs mention `discoveryOk`, `planningOk`, and `workerCount`, then remove the completed backlog item. GitHub: *@jtenner*
- **docs: annotate the host types with the ready-work scheduling policy** Add JSDoc to the shared `workerCount` fields so the `harness/shared/harness-types.d.ts` surface explicitly states that ready work consumes same-machine execution slots, and remove the completed backlog item. GitHub: *@jtenner*
- **docs: let the module-global scheduler fan out ready work in parallel** Clarify in the host-runner contract, Vitest scheduling note, and README that shipped `start()` keeps deterministic ordering but uses same-machine worker slots for independent ready work, and remove the completed backlog item. GitHub: *@jtenner*
- **docs: pin declaration-order tie-breaking for ready scheduling** Clarify in the host-runner contract and README that declaration order is the stable ready-queue tie-breaker across branches after dependency and sequential constraints, and remove the completed backlog item. GitHub: *@jtenner*
- **docs: codify the dependency-cycle blocked diagnostic shape** Clarify in the host-runner contract and README that cycle members block as `dependency-cycle` with an explicit blank dependency identity key, and remove the completed backlog item. GitHub: *@jtenner*
- **docs: finalize dependency outcome wording and retire the backlog item** Clarify in the root README and host-runner contract that failing or trapping prerequisites block dependents transitively, while skip/todo/filtered prerequisites surface as `missing-dependency`, and remove the completed live-backlog entry. GitHub: *@jtenner*
- **reporter: surface shared run metadata and packaged command results** Add a module-global `metadata` snapshot to the shared start result, thread that metadata through the CLI run report, teach the packaged command runner to persist its JSON payload to a caller-provided result path, and refresh the smoke coverage and host contract notes to match the new orchestration surface. GitHub: *@jtenner*

## 2026-03-19

- **docs: keep targeted replay as the current `v0.3.0` execution primitive** Update the ABI, guest-runtime, host-runner, and AssemblyScript package docs to state that scheduler-step entrypoints remain deferred for the current `v0.3.0` direction while host-owned scheduling still executes planned tests through targeted `run(nodeIndex)` replay, extend shared `start()` proof to assert the executor actually calls the planned node-index handle, and remove the completed backlog decision item. GitHub: *@jtenner*
- **proof: lock cycle and duplicate-edge scheduling semantics through the shipped CLI** Add cross-host CLI coverage that proves duplicate dependency edges collapse without changing outcomes and that user-declared cycles report distinct `dependency-cycle` blocked diagnostics, update the host-runner and README contracts to document duplicate-edge collapse, cycle blocking, and declaration-order tie-breaking, and remove the completed graph-semantics backlog item. GitHub: *@jtenner*
- **proof: distinguish packaged verifier supervision failures from real packaged command failures** Refactor `verify-packaged-cli.ts` to export explicit failure renderers, label packaged timeouts as likely bundled-host hangs rather than verifier-wrapper bugs, mark verifier-wrapper failures separately, surface timeout status in the Markdown report summary, add direct regression coverage for those failure classes, update the release/scripts docs, and remove the now-complete packaged verification backlog item. GitHub: *@jtenner*
- **proof: lock the documented dependency-policy matrix across shared start and CLI reporting** Add a focused cross-host shared-smoke assertion that `start()` preserves the documented `skip`, `todo`, `only`-filtered, satisfied expected-failure, and unexpected-pass dependency outcomes, add a matching CLI matrix test across the shipped hosts so reporter counts and diagnostics stay aligned with that contract, update the host-runner proof note, and remove the completed backlog item. GitHub: *@jtenner*
- **proof: make host verification reuse the package-local smoke commands** Add a shared source-host smoke-command helper, switch the root `bun test` flow and `verify:source-hosts` to execute each harness package's `npm test` command instead of duplicating lower-level build/test steps, update the repo and CLI docs to describe the aligned local/CI proof path, and remove the completed wrapper-drift item from the live backlog. GitHub: *@jtenner*
- **docs: add publish workflow rules to AGENTS** Document the release-specific workflow in `AGENTS.md`, including the required semver bump prompt, `bun validate` gate, aligned package version bumps, temporary changelog summary reuse for annotated tags and GitHub releases, push/release creation behavior, CI monitoring, and final reporting expectations. GitHub: *@jtenner*
- **scheduler: make expected-failure execution semantics match dependency planning** Treat `expectFailure` executions that fail as expected as semantic success in shared `start()` results, branch `ok`, and CLI reporting, while unexpected passes now report as real failures that can still block dependents; add reporter unit coverage, cross-host CLI proof for the satisfied-prerequisite path, update the guest smoke fixtures to use honest xfail callbacks where needed, and remove the now-completed dependency-policy proof item from the live backlog. GitHub: *@jtenner*
- **tooling: add a repo-wide format command and formalize the commit workflow** Add root `bun format` orchestration that runs Biome across the repo's JS/TS/JSON sources plus `gofmt` and `cargo fmt` for the native hosts, wire `bun validate` through format-check mode before the CLI lint pass, document the new command in the root and scripts READMEs, record the remaining AssemblyScript decorator-formatting gap in `agent-todo.md`, and split `AGENTS.md` into explicit commit and research process sections that now require `bun format` before validation and test execution. GitHub: *@jtenner*
- **proof: extend CLI dependency planning coverage across all shipped hosts** Run the real guest-declared dependency-handle CLI suite against `js`, `wazero`, and source-built `wasmtime` so success, blocked-dependency, missing-dependency, unexpected-pass, and `only`-filtered prerequisite reporting are all proved through the shipped host/runtime paths; update the host-runner contract proof section; and narrow the live backlog by removing the completed non-JS CLI blocked/planning proof gap. GitHub: *@jtenner*
- **proof: add cross-host Vitest sequential smoke coverage** Extend the shared harness smoke suite with real `vitest` discovery and `start()` assertions so sequential declaration metadata and nested execution shape are proved against the compiled guest adapter, add packaged CLI `vitest` run coverage for the `wazero` and `wasmtime` hosts, and narrow the live backlog by removing the completed cross-host sequential smoke gap. GitHub: *@jtenner*
- **hosts: make in-band shared `start()` execution the shipped cross-host contract** Switch the `js` and `wasmtime` host wrappers onto the same in-band shared `start()` mode already used by `wazero`, update the JS package-local smoke to assert main-thread execution instead of worker-thread hops, refresh the host-contract and package README wording to describe `workerCount` as execution slots satisfied in-band by all shipped hosts, and remove the now-completed execution-mode decision from the live backlog. GitHub: *@jtenner*
- **proof: broaden shared scheduler coverage for global staging and blocked propagation** Add shared planner tests that prove module-global declaration-order tie-breaking across branches, broader topological staging through explicit dependency edges, and blocked propagation that leaves unrelated ready work satisfied while only downstream dependents are suppressed; then narrow the backlog by removing the completed host-level scheduler-proof gap from the remaining verification matrix. GitHub: *@jtenner*
- **proof: lock repeated `start()` stability across all shipped hosts** Add shared host smoke coverage that calls `start()` twice on the same harness instance and asserts the discovery tree, planning diagnostics, blocked nodes, and execution summaries remain identical across the two runs, then narrow the backlog by removing repeated-`start()` stability from the remaining scheduler-proof gaps. GitHub: *@jtenner*
- **docs: publish the proved dependency policy in the runner contract and READMEs** Document in the shared host-runner contract that dependency node ids resolve through ancestry scope, that `skip`, `todo`, and `only`-filtered prerequisites report as `missing-dependency`, and that `expectFailure` prerequisites satisfy dependents only when they fail as intended; add matching user-facing notes to the root, assembly, and CLI READMEs; and narrow the live backlog from undocumented dependency semantics to the remaining repeated-`start()` and broader scheduler-proof gaps. GitHub: *@jtenner*
- **scheduler: resolve dependency node ids by scope instead of global reuse** Fix the shared host planner so `dependencyNodeIds` resolve through the dependent node’s ancestry scopes instead of a single global `nodeId` map, which prevents `only`-filtered or otherwise missing nested prerequisites from accidentally binding to unrelated nodes that reuse the same local `nodeId`; add pure planner coverage for repeated local-id collisions, extend the real `node:test` smoke and CLI coverage with an `only`-filtered prerequisite case, refresh the synthetic dependency-outcome harness to follow the same ancestry rules, regenerate the bundled virtual AssemblyScript sources, and narrow the backlog away from `only`-policy proof toward documenting the now-proved dependency contract. GitHub: *@jtenner*
- **scheduler: prove guest-declared dependency policy for skip, todo, and unexpected-pass prerequisites** Extend the shared guest `node:test` smoke module with dependencies on skipped and todo declarations plus an expected-failure prerequisite that unexpectedly passes, update the shared host smoke metadata and `start()` assertions so `js`, `wazero`, and `wasmtime` prove those planner outcomes through real guest discovery, add CLI run coverage for `missing-dependency` and unexpected-pass blocked reporting, regenerate the bundled virtual AssemblyScript sources, and narrow the backlog from those dependency-policy proofs to the remaining `only` interaction. GitHub: *@jtenner*
- **scheduler: prove guest-declared blocked dependencies through real node:test smoke** Extend the shared guest `node:test` smoke module with a failing prerequisite, a blocked dependent, and an expected-failure prerequisite that still satisfies its dependent when it fails as intended; update the shared host smoke metadata and `start()` assertions so `js`, `wazero`, and `wasmtime` all prove real guest-authored blocked/planning outcomes rather than only synthetic planner fixtures; add a CLI run test for the blocked reporter path; regenerate the bundled virtual AssemblyScript sources; and narrow the backlog to the remaining skip/todo and unexpected-pass prerequisite policies. GitHub: *@jtenner*
- **node:test: expose chainable dependency declaration handles** Change the guest `node:test` adapter so `test(...)`, `it(...)`, and the related modifier aliases return chainable declaration handles with `.dependsOn(...)`, route those handles onto the already-stable guest dependency node IDs, prove the surface in internal AssemblyScript tests plus a CLI compile/run test, extend the real guest `node:test` smoke module with dependency declarations so cross-host discovery and `start()` now carry guest-authored `dependencyNodeIds`, regenerate the bundled virtual AssemblyScript sources, and narrow the backlog from “no declaration API” to guest-declared blocked/planning semantics. GitHub: *@jtenner*
- **proof: share dependency planning smoke coverage across all shipped hosts** Move the fake-harness dependency planner fixtures out of the JS package and into shared harness test support, add a shared start-planner smoke suite that proves missing dependency diagnostics and execution-time blocked propagation through `decorateHarness`, run that suite from the `js`, `wazero`, and `wasmtime` host packages using each host's actual shared-start execution mode, and update the live backlog to shift the remaining proof gap from non-JS parity to true guest-declared dependency coverage. GitHub: *@jtenner*
- **wasmtime: restore macos arm64 source-host builds through napi linker setup** Add the standard Rust `napi-build` hook to `harness/wasmtime` so macOS source-host builds link as real Node-API addons instead of failing with unresolved `napi_*` symbols on arm64 runners, refresh the package README to call out the platform-specific linker setup in the local build helper, and trim the now-completed packaged `wazero` timeout follow-up out of the live backlog. GitHub: *@jtenner*
- **wazero: bypass bundled close teardown in packaged CLI runs** Treat packaged `wazero` harness instances as process-lifetime resources inside the bundled CLI instead of synchronously closing each native host, because the remaining hosted Linux x64 failure is a packaged-only timeout before any reporter output while source-host `wazero` still passes; add opt-in `AS_HARNESS_TRACE_WAZERO=1` phase logs plus verifier-side diagnostic reruns so any remaining hosted timeout reports whether the hang is in bundled harness load, native host creation, `start()`, or packaged shutdown. GitHub: *@jtenner*
- **release: stop packaged CLI verification from waiting on leaked pipe handles** Replace the packaged verification Node wrapper's pipe-based `spawnSync(...)` supervision with a file-backed async runner plus explicit timeout/tree-kill handling, add regression coverage that simulates a passing child process whose descendant keeps inherited stdio handles open, and narrow the backlog note so the remaining Windows packaged smoke question is whether Bun's standalone executable still fails to exit once verifier pipe capture is out of the way. GitHub: *@jtenner*
- **wazero: route bundled runtime through shared in-band start semantics** Update the bundled CLI `wazero` runtime to decorate packaged harnesses through the shared `start()` contract with in-band execution instead of bypassing the shared planner, add direct shared coverage proving `decorateHarness(..., { runInBand: true })` can execute `start()` and merge coverage snapshots without worker threads, clarify in the host-runner contract docs that `workerCount` reports execution slots rather than necessarily OS worker threads, and update the live backlog to keep the remaining packaged timeout/shutdown hang visible. GitHub: *@jtenner*
- **hosts: restore wazero shared-start parity and expand local host validation** Route `harness/wazero` through the shared `start()` contract with an in-band execution mode that avoids the worker-thread deadlock, fix the native addon regressions by restoring the correct boolean helper and exposing persistent coverage snapshots/reset methods, and update the root test and source-host verification scripts to run the actual package host smoke commands directly so `js`, `wazero`, and `wasmtime` regressions surface in normal local validation. GitHub: *@jtenner*
- **scheduler: skip blocked dependents during live stage execution** Update the shared `start()` executor so dependency outcomes are applied stage by stage instead of after every planned test has already run, which means unsatisfied prerequisites now suppress downstream dependents before execution while expected-failure prerequisites still satisfy dependents when they fail as intended; add JS-host smoke proof for both cases and keep the remaining host-parity work focused on non-JS `start()` behavior. GitHub: *@jtenner*
- **scheduler: wire dependency node IDs through discovery and host planning** Extend the guest `NodeFound` ABI so discovered nodes now carry `dependencyNodeIds`, preserve that metadata across replayed declaration slots and all shipped host decoders, teach the shared planner to resolve dependency node IDs onto stable execution identities while keeping the synthetic planner-test path, add guest serialization/replay proof plus shared and JS-host dependency-blocking coverage, refresh the ABI/host contract docs, and regenerate the bundled virtual AssemblyScript sources. GitHub: *@jtenner*
- **hosts: surface planning issues and blocked nodes through `start()` and the CLI reporter** Extend the shared `start()` result with `planningOk`, planner issues, and blocked-node data, keep discovery failures distinct from planning failures, update the CLI run reporter and exit handling so blocked tests can become first-class failures once dependency metadata lands, add reporter unit coverage, and strengthen the shared smoke contract to prove the new result fields stay empty on the current no-dependency path. GitHub: *@jtenner*
- **scheduler: model prerequisite satisfaction and blocked propagation in pure host logic** Extend the shared scheduler helpers so pure host-planner tests can classify prerequisite satisfaction, treat expected-failure nodes as satisfied only when they fail, and propagate blocked dependents from unsatisfied prerequisites in planner order; add shared planner proofs for expected-failure prerequisite handling and downstream blocked propagation; and update the live backlog so the remaining work is wiring those semantics into ABI-emitted dependency metadata and reporter-facing blocked outcomes. GitHub: *@jtenner*
- **scheduler: make the shared planner dependency-ready with pure diagnostics** Extend the shared `start()` planner so synthetic host-planner inputs can express dependency edges by stable identity, collapse duplicate dependency edges, block missing prerequisites transitively, and report cycle members after planning ready nodes; add pure shared planner tests for missing dependencies and dependency cycles; and update the live backlog so the remaining dependency work is on ABI emission, blocked outcomes, and reporter-facing semantics rather than planner shape alone. GitHub: *@jtenner*
- **hosts: carry `only` and expected-failure intent through discovery snapshots** Extend the guest `NodeFound` payload and all three host decoders so discovered nodes now preserve `only` and expected-failure metadata alongside stable IDs, declaration order, and sequence mode; add a real `test.expectFailure(...)` declaration to the shared `node:test` smoke module; regenerate the bundled virtual AssemblyScript sources; and strengthen the shared smoke expectations so the new metadata is proven across discovery and `start()` results. GitHub: *@jtenner*
- **scheduler: execute the discovered graph through one shared worker** Change the shared `start()` executor to flatten the module-global execution plan into one deterministic queue, run that queue through exactly one worker thread so `v0.3.0` scheduling semantics match the current design docs, tighten the shared and JS host smoke expectations around `workerCount === 1`, and update the backlog so the remaining scheduler work is blocked/dependency semantics rather than more branch-parallel cleanup. GitHub: *@jtenner*
- **scheduler: move planner proof into shared harness coverage** Add `harness/shared/start.test.cjs` so the module-global execution planner is exercised directly by the repo-wide shared test suite instead of only through JS-host smoke coverage, trim the duplicated synthetic planner assertions from the JS host smoke file, and update the live backlog so the remaining proof work is on broader graph semantics and cross-host coverage. GitHub: *@jtenner*
- **scheduler: lower sequential intent onto runnable-test edges** Refine the shared `start()` planner so explicit sequential intent no longer falls back to top-level branch barriers; instead it now derives ordering from runnable ancestor relationships plus explicit sequential scopes, adds deterministic planner proof in the JS host smoke suite, and leaves the remaining scheduler work focused on broader proof and future dependency-edge semantics. GitHub: *@jtenner*
- **scheduler: plan execution as module-global test stages** Replace whole-branch execution units in the shared `start()` planner with module-global runnable-test stages, keep declaration-order edges within each discovered branch, preserve the existing top-level sequential barriers as cross-branch edges, and narrow the remaining scheduler work to replacing those coarse edges with minimal per-scope sequential constraints. GitHub: *@jtenner*
- **scheduler: narrow sequential fallback to top-level branch barriers** Replace the coarse single-worker `sequenceMode` fallback in the shared `start()` planner with staged execution that still treats top-level sequential branches as barriers but preserves parallel branch execution before and after them, update the JS host smoke fixtures to prove the barrier behavior, and tighten the backlog so the remaining scheduler work is specifically on lowering sequential intent onto runnable tests inside mixed branches. GitHub: *@jtenner*
- **scheduler: carry Vitest sequence intent into host planning** Add ancestry-based declaration keys in the shared `start()` planner so deep replayed descendants no longer alias under repeated local IDs, transport explicit `sequenceMode` metadata from the guest Vitest declarations through `NodeFound` and all three hosts, regenerate the bundled AssemblyScript virtual sources, strengthen host smoke coverage for deep identity collisions and the new single-worker sequential fallback, and update the backlog to shift the remaining work toward minimal ordering constraints instead of metadata plumbing. GitHub: *@jtenner*
- **hosts: expose stable test identity through discovery snapshots** Extend the guest `NodeFound` event payload with `nodeId`, `parentNodeId`, and declaration-order metadata, decode and preserve those fields across the `js`, `wazero`, and `wasmtime` hosts plus the shared `start()` clones, regenerate the bundled AssemblyScript virtual sources, strengthen shared smoke expectations to prove the new identity fields, and update the backlog so the remaining work moves from ABI exposure to planner adoption. GitHub: *@jtenner*
- **assembly: assign durable declaration identities before scheduling work** Refactor the guest runtime so tests and suites receive stable `nodeId` and declaration-order metadata from durable declaration slots instead of replay allocation order, reject replay-shape drift rather than silently remapping identity, add internal coverage proving stable IDs survive repeated replay, and update the live backlog to treat the remaining identity work as ABI and host-planner propagation. GitHub: *@jtenner*
- **docs: serialize doc filenames and define the research-doc contract** Rename every tracked `docs/` file to the shared `[serial]-[YYYY-MM-DD]-[kebab-case-title].md` format, update the repo-wide links and plain-text references that pointed at the old names, and extend `AGENTS.md` so future research is detailed, project-relevant, stored under `docs/` with the same naming scheme, and easy to identify from its title and opening summary. GitHub: *@jtenner*
- **docs: tighten the live backlog structure for upcoming work only** Add `docs/009-2026-03-19-vitest-scheduling-and-test-graph-strategy.md` as a detailed plan for stable test identifiers, graph-aware scheduling, blocked semantics, and the host-owned scheduler work needed after the thin `"vitest"` adapter surface; rewrite `agent-todo.md` into a succinct unreleased-only backlog organized by upcoming version, then `Blockers`, `Risks`, and feature sections with only remaining work; and update `AGENTS.md` to require that structure so the backlog stays a true todo list instead of mixing in released-scope notes or retrospective content. GitHub: *@jtenner*
- **vitest: add low-risk sequential aliases to the thin adapter** Extend the shipped `"vitest"` guest adapter with `test.sequential`, `it.sequential`, `describe.sequential`, and `suite.sequential` as honest aliases of the current always-sequential runner behavior, add internal AssemblyScript coverage plus guest and CLI smoke usage for the new names, and update the Vitest docs, README set, backlog note, adapter TODO, and changelog to describe the broader but still intentionally small surface. GitHub: *@jtenner*
- **docs: remove the stale completed-item backlog rule from AGENTS** Delete the older `AGENTS.md` line that told contributors to mark completed `agent-todo.md` items before commit, because it conflicted with the newer directive to investigate the backlog, remove completed items, and update blockers or risks instead. GitHub: *@jtenner*
- **docs: trim completed backlog items and tighten the commit-time backlog rule** Remove the completed coverage, host-runtime, ABI, release-engineering, traversal, runtime-state, and contract checklist entries from `agent-todo.md` so it reads as a live remaining-work backlog again, add current blocker/risk notes for the open strict-equality and framework-adapter areas, and update `AGENTS.md` to require investigating `agent-todo.md` before every commit so completed items are removed and new blockers or risks are captured. GitHub: *@jtenner*
- **vitest: ship the first thin guest adapter slice** Add `assembly/assembly/vitest/` plus the bundled `"vitest"` library entry point, reuse the shipped Jest matcher subset for `expect(...)`, expose `test` / `it` / `describe` / `suite`, `fails`, `skipIf` / `runIf`, hooks, and `assertType(...)`, add internal AssemblyScript coverage plus guest and CLI smoke tests, regenerate the bundled virtual sources, and update the README set, roadmap, adapter TODO, and changelog to document the new supported surface. GitHub: *@jtenner*
- **repo: ignore the `wasmtime` build cache and document cache-ignore hygiene** Add `harness/wasmtime/.cache/` to the package-level ignore rules so local Rust/Node build cache material stops showing up as untracked repo noise, and update `AGENTS.md` so newly discovered build/cache directories are explicitly treated as ignore candidates when they are not meant to be tracked. GitHub: *@jtenner*
- **docs: define the recommended `vitest` adapter surface and limits** Add `docs/008-2026-03-19-vitest-adapter.md` as a detailed implementation research guide for the next guest test adapter, choose `vitest` as the best post-`node:test` / post-`jest` target, map the upstream API into `Ship now` / `Later` / `Skip` buckets against the current shared Wasm runtime, and record exactly which pieces should be deferred or rejected because they depend on closures, Promises, dynamic mocks, fixtures, or other AssemblyScript-misaligned features. GitHub: *@jtenner*

## 2026-03-17

- **ci: stop the root Bun test job from assuming a prebuilt `wasmtime` addon** Gate the CLI `wasmtime` coverage smoke in `cli/run.test.ts` on the presence of `harness/wasmtime/dist/wasmtime.node` so the clean-runner `validate-and-test` job no longer fails before the dedicated source-host matrix has a chance to build and prove the Rust-backed host, then reissue the `v0.2.0` tag from that fix. GitHub: *@jtenner*
- **docs: fix the bundled jest usage guidance before reissuing `v0.2.0`** Correct the repo, CLI, assembly, and dedicated Jest adapter docs so they no longer claim `--lib jest` is required, update the end-to-end CLI Jest smoke to prove plain `import { ... } from "jest"` works through the bundled guest library, and republish `v0.2.0` from the corrected release commit. GitHub: *@jtenner*
- **release: stabilize packaged CLI verification for `wazero` on linux x64** Refresh the staged current-target `wazero` addon instead of reusing any stale local `cli/n-api` copy, and stop `verify-packaged-cli.ts` from supervising packaged Bun executables directly through Bun child-process capture by delegating each smoke command to a Node wrapper process; together those changes make the packaged `bun-linux-x64` `wazero` smoke pass reliably again and restore the missing packaged verification reports. GitHub: *@jtenner*
- **release: prepare the repository metadata for `v0.2.0`** Bump `cli/package.json` to `0.2.0` so the release workflow will accept a `v0.2.0` tag, refresh the main README scope wording to describe the current release line instead of the original `v0.1.0` wording, and update the release-process guide examples to use the `0.2.0` tag and version pair. GitHub: *@jtenner*
- **docs: define the shipped host-runner contract and close its blocker** Add a dedicated `docs/007-2026-03-17-host-runner-contract.md` guide for the public harness surface, callback-slot registration semantics, direct method behavior, and `start()` orchestration rules; extend the shared smoke suite to prove callback registration replaces the active event-slot listener across hosts; update the repo, assembly, ABI, and release docs to point at the new runner contract; and mark the host-runner blocker complete while removing the two broader ABI-general blockers from the backlog. GitHub: *@jtenner*
- **node:test: make targeted discovery emit every structurally visible node in its slice** Tighten successful non-root `discover(nodeIndex)` so it emits the resolved target node together with any structurally visible immediate children under that target, keep the raw Wasm return value as the immediate-child count, update the shared host start-orchestration helper to ignore replayed branch roots, extend the shared host smoke and internal traversal coverage for the new targeted-discovery shape, and mark the remaining `NodeFound` discovery backlog item complete. GitHub: *@jtenner*
- **assembly: make targeted replay state scratch-only and trap-safe** Move non-root traversal replay onto per-node scratch buffers for children, hooks, and declaration-mode changes instead of mutating durable node structure, clean that replay state after successful and trapping discovery/run attempts through the guest trampoline boundary, add internal traversal coverage that proves trap recovery leaves no replay buffers behind, and regenerate the bundled AssemblyScript virtual sources so packaged compilation uses the same replay semantics. GitHub: *@jtenner*
- **assembly: track active node paths, hook phases, and traversal targets during execution** Extend the guest execution-state layer with tracked active node-index and traversal-target snapshots plus lifecycle hook phase metadata, wire the executor and traversal helpers to set and clear that state around hook, node, discovery, and targeted run callbacks, add dedicated internal AssemblyScript coverage for those state views and cleanup paths, and update the AssemblyScript test bootstrap to provide the real staged-callback trampoline import needed by the expanded guest tests. GitHub: *@jtenner*
- **docs: pin the targeted traversal contract for `NodeIndex`, `only`, `skip`, and `todo`** Document the exact staged traversal rules in the harness ABI and guest-runtime contract guides, including the distinction between an unstaged export call and an empty staged `NodeIndex`, make root-target, `only`-filtered, `skip`-pruned, and `todo`-descendant behavior explicit, extend the guest traversal tests plus shared host smoke coverage to lock those semantics in, and mark the corresponding traversal-contract backlog items complete. GitHub: *@jtenner*
- **docs: write the guest runtime module contract and defer scheduler-step entrypoints** Add a dedicated `docs/006-2026-03-17-guest-runtime-contracts.md` reference covering the current `api`, `registry`, `traversal`, `executor`, `hooks`, `assert_bridge`, `events`, `abi`, and `state` ownership boundaries; wire that guide into the repo docs; make the current flat-ABI stance explicit by documenting scheduler-step entrypoints as deferred beyond `v0.2.0`; and mark the corresponding contract backlog items complete. GitHub: *@jtenner*
- **wazero: stabilize packaged and source CLI shutdown on linux x64** Release the native compiled module before closing the wazero runtime, stop the native `start()` path from fanning branch work out through Go goroutines, and force the CLI entrypoint to exit explicitly after `main()` completes so the intermittent Linux x64 `wazero` smoke hangs no longer stall packaged verification or package-local CLI tests. GitHub: *@jtenner*
- **release: make the packaged-binary policy explicit** Define the current release policy directly in the repo docs and backlog: downloadable Bun executables are the official release channel, `npm` publication is not a current goal, packaged releases include `js` and `wazero` only, `wasmtime` remains source-only, and the current CI plus clean-environment verification flow is treated as sufficient release proof. GitHub: *@jtenner*
- **hosts: close the source-host proof contract for js, wazero, and wasmtime** Make the Node.js 22 source-host baseline explicit in the shared target metadata, align the docs and backlog with that decision, treat the current CI source-host matrix as the proof contract for `js`, `wazero`, and `wasmtime` across the supported hosted runners, and tighten the CI artifact naming so the persisted proof reports include both the matrix label and Node version. GitHub: *@jtenner*
- **release: verify packaged cli targets from clean staged installs** Strengthen `verify-packaged-cli.ts` so it no longer proves packaged executables only while they still sit beside the repo checkout; instead it now stages each release-named executable into a temporary install-like directory, runs the smoke suite from a separate temporary project directory, emits per-target JSON and Markdown proof reports, wires those reports into the CI and release workflows, and updates the backlog plus release docs to mark the clean-environment packaged verification slice complete. GitHub: *@jtenner*
- **coverage: finish the `v0.2.0` control and report contract** Implement bundled AssemblyScript coverage declarations plus a coverage transform, wire the `js`, `wazero`, and `wasmtime` hosts to collect and merge coverage through `start()`, add `text` / `json` / `yaml` / `csv` / `lcov` / `cobertura` output, ship CLI-level `--coverage-include`, `--coverage-exclude`, and repeated `--coverage-point-type` controls, preserve empty enabled coverage snapshots instead of collapsing them to `null`, regenerate the bundled virtual transform assets, and update the README set, ABI guide, and backlog to reflect the completed `v0.2.0` coverage contract. GitHub: *@jtenner*
- **jest: make skip aliases explicit in smoke coverage and docs** Tighten the Jest smoke fixtures so `xtest(...)`, `xit(...)`, `xdescribe(...)`, and `test.todo(...)` callbacks hard-fail if they ever execute, extend the end-to-end CLI `--lib jest` smoke with explicit skip-alias callbacks, regenerate the bundled AssemblyScript sources, and update the README set plus `docs/005-2026-03-17-jest-adapter.md` so the alias-to-skip/only mapping and skip-pruning semantics are documented directly. GitHub: *@jtenner*
- **assembly: expand the jest expect surface with container and numeric matchers** Extend the thin Jest adapter with `toContain(...)`, `toContainEqual(...)`, `toHaveLength(...)`, `toBeGreaterThan(...)`, `toBeLessThan(...)`, and `toBeNaN()` plus their `.not` variants, reuse the shared equality bridge for container semantics, prove the new matcher set through the guest Jest smoke and the end-to-end CLI `--lib jest` path, regenerate the bundled AssemblyScript sources, and update the README set, adapter guide, backlog note, adapter TODO, and changelog to reflect the broader matcher surface. GitHub: *@jtenner*
- **docs: add a dedicated jest adapter guide** Add `docs/005-2026-03-17-jest-adapter.md` covering the current declaration surface, supported `expect(...)` matchers, `toThrow()` trap semantics, callback-shape limitations, and AssemblyScript string notes, then link that guide from the repo, assembly, CLI, roadmap, backlog, and adapter TODO docs so the supported Jest-shaped guest API is documented in one place. GitHub: *@jtenner*
- **assembly: support nested trap observation and jest toThrow** Allow the guest trampoline to stage nested callback probes so trap-observing assertions can run inside already-running test callbacks, add `expect(...).toThrow()` and `.not.toThrow()` to the thin Jest adapter, extend the trampoline and CLI smoke coverage for the new behavior, and update the README set, backlog note, adapter TODO, generated bundle, and changelog to describe the expanded matcher surface. GitHub: *@jtenner*
- **assembly: extend the thin jest adapter with core expect matchers** Add a small shared-assertion-backed `expect(...)` surface for the Jest adapter covering strict/deep equality, truthy/falsy, and nullish checks, prove it through the CLI `--lib jest` path, and update the README set, backlog, adapter TODO, generated bundle, and changelog to describe the supported matcher subset and the remaining `toThrow()` / async / mock gaps. GitHub: *@jtenner*
- **assembly: add a thin jest declaration adapter** Add a new `assembly/assembly/jest/` entry point plus `lib/jest.ts`, wire `--lib jest` through the bundled compiler-library path, add compile and CLI smoke coverage for the basic Jest-shaped `test` / `it` / `describe` / hook surface, and update the roadmap, README set, adapter TODO, backlog, and changelog to describe the new thin adapter and its deferred matcher/mock work. GitHub: *@jtenner*
- **cli: add deterministic reporter trees and first-class trace logs** Add a shared `log` host event for intercepted AssemblyScript `trace(...)` calls across the `js`, `wazero`, and `wasmtime` harnesses, introduce a CLI reporter contract that consumes a deterministic `HarnessStartResult`-derived tree instead of formatting directly from live callbacks, update the default `run` reporter to print pass/fail/discovered counts plus failed-test logs only, extend the shared smoke fixtures and CLI tests for the new behavior, and align the ABI plus README set with the expanded reporting surface. GitHub: *@jtenner*
- **ci: add explicit source-host verification reports per matrix target** Extend the shared host-validation metadata with the Node version used for each target, add a `verify-source-hosts.ts` helper that runs the expected `js`, `wazero`, and `wasmtime` package checks for one matrix label and writes JSON/Markdown proof reports, update the CI workflow to use that helper and upload the reports as artifacts, and align the docs plus backlog with the more concrete host-proof path. GitHub: *@jtenner*
- **docs: collapse agent-todo into a live backlog** Rewrite `agent-todo.md` so it no longer carries completed checklist history inline, keep the accepted `v0.1.0` scope and deferred-decision notes at the top, and leave only the still-open publishing, ABI, strict-equality, adapter, traversal, and runtime-state work as the active backlog. GitHub: *@jtenner*
- **ci: split the source-host validation matrix from the packaged release matrix** Replace the single Ubuntu-only host-smoke path with an explicit source-host matrix emitted from shared metadata, run the `js`, `wazero`, and `wasmtime` harness smoke suites across the intended hosted runners, keep packaged artifact verification on the narrower proven release-target matrix, update the repo docs and scripts to explain the distinction, and extend the release notes generator plus backlog to track the new source-host proof surface. GitHub: *@jtenner*
- **harness: add a Rust-backed wasmtime host and CI coverage** Add a new `harness/wasmtime` package that builds a Rust `Node-API` addon on top of `wasmtime`, surfaces the shared host contract through a JS wrapper plus the shared start/smoke infrastructure, wires `--harness wasmtime` into the CLI source runtime selection, installs Rust in the CI and release workflows, updates the README set and ABI docs for the new source-only host, and marks the corresponding source-host backlog item complete in `agent-todo.md`. GitHub: *@jtenner*
- **docs: replace local filesystem markdown links with repo-relative links** Rewrite the tracked Markdown links that still pointed at `/home/jtenner/Projects/as-harness/...` so the README family, ABI docs, release docs, notices, and related package docs now use repository-relative paths that resolve correctly on GitHub, and normalize the release-process shell example so it no longer hard-codes a local checkout path. GitHub: *@jtenner*
- **release: keep packaged linux arm64 on the proven js-only surface** Update the packaged target map so `bun-linux-arm64` no longer declares bundled `wazero` support, because the hosted packaged `wazero` smoke is still timing out on Bun's standalone arm64 executable path; keep the release target itself, keep source-based arm64 `wazero` support, and align the runtime-target tests plus release docs with the narrowed packaged contract. GitHub: *@jtenner*
- **license: add the project MIT license and align package metadata** Add a root MIT `LICENSE`, mark the package manifests as MIT-licensed, and update the main docs plus third-party notices so the project license is explicit alongside the release-time dependency notice bundle. GitHub: *@jtenner*
- **release: stage third-party notices and unblock Bun wazero release smoke** Make the CLI `wazero` runtime load lazily so Bun no longer hangs while resolving it during `runEntryFiles(...)`, harden packaged smoke verification with a subprocess timeout, add tracked third-party notice and license files plus a release-legal staging script, and wire the release workflow/docs to publish that legal bundle with each release artifact set. GitHub: *@jtenner*
- **docs: refresh markdown for the release-ready project state** Tighten the root, assembly, wazero, and native-addon staging READMEs so they reflect the current hosted release matrix, packaged Windows `js`-only behavior, the remaining clean-environment proof work, and the post-CI release posture instead of older “still being wired up” wording. GitHub: *@jtenner*
- **release: tighten versioning and published artifact metadata for v0.1.0** Set the CLI package version to `0.1.0`, mark the shipped harness-selection policy complete in `agent-todo.md`, add a dedicated release-process operator guide, and extend the release-manifest generator so publish-time metadata now enforces tag/version consistency and emits `SHA256SUMS.txt` alongside `release-manifest.json`. GitHub: *@jtenner*
- **harness: surface structured node and callback failures through the ABI** Add guest-side `nodeFail` and `callbackFail` event emission with explicit `FailureKind` tagging for assertion versus trap paths, extend both shipped hosts and the shared smoke suite to decode and assert those failure events, update the harness ABI reference to document the new wire payloads and callback surface, and mark the remaining failure-propagation checklist items complete in `agent-todo.md`. GitHub: *@jtenner*
- **harness: add explicit close semantics to prevent wazero test hangs** Add `close()` to the shared host interface, implement deterministic native-runtime release in the `wazero` addon, wire the CLI and shared worker/start orchestration to close harnesses explicitly instead of relying on GC finalizers, track and close harnesses in the shared smoke suite, and document the new lifecycle expectation in the harness ABI plus host READMEs so the wazero smoke path exits cleanly in CI. GitHub: *@jtenner*
- **harness: document and prove todo-node execution suppression** Extend the shared `node:test` smoke fixture with a top-level `todo` leaf, add internal executor coverage that `todo` nodes do not execute, expand the shared host smoke to prove `todo` nodes stay discoverable while suppressing their own execution events and `start()` significance, update the harness ABI notes to spell out the host scheduling expectations for `todo`, and mark the corresponding backlog items complete in `agent-todo.md`. GitHub: *@jtenner*
- **harness: prove replay determinism and branch pruning through fixtures** Extend the internal traversal tests with a deterministic nested `describe` -> `test` -> `test` replay case, add a shared host smoke fixture that traps during child discovery so branch pruning and same-harness recovery are exercised through both host implementations, expand the `start()` assertions for the pruned branch outcome, and mark the corresponding replay/pruning checklist items complete in `agent-todo.md`. GitHub: *@jtenner*
- **harness: add hook-failure and trap-recovery smoke coverage** Extend the shared `node:test` smoke fixture with a failing `beforeEach` branch and a trapping nested test, expand the shared host smoke assertions to prove skip-pruned discovery, lifecycle callback failure propagation, and same-harness recovery after a trapped run across both `js` and `wazero`, and mark the completed fixture-coverage checklist items in `agent-todo.md`. GitHub: *@jtenner*
- **cli: remove the static source wazero require edge from packaged builds** Update the `wazero` runtime loader to resolve the source-tree fallback through `createRequire(import.meta.url)` and a non-literal specifier instead of a plain top-level `require("../../harness/wazero/index.cjs")`, which keeps source-mode development intact while preventing Bun's packaged build graph from eagerly resolving `harness/wazero/dist/wazero.node` for `js`-only targets. GitHub: *@jtenner*
- **cli: avoid bundling the source wazero addon into packaged js-only targets** Update the `wazero` runtime loader so the source-tree fallback `require("../../harness/wazero/index.cjs")` is only taken when no packaged `WAZERO_TARGET` constant exists, which lets Bun dead-code-eliminate the source addon path for packaged targets such as Windows `js`-only artifacts that should no longer look for `harness/wazero/dist/wazero.node` at build time. GitHub: *@jtenner*
- **release: generate artifact metadata and notes from the shared target map** Extend the release-target metadata with explicit packaged-harness support, teach the packaged smoke helper to use that metadata instead of inferring `wazero` support indirectly, add a `scripts/release-manifest.ts` generator that writes `release-manifest.json` plus release notes for a tagged build, wire the publish workflow to generate and apply those notes before creating or updating the GitHub release, and update the docs plus release checklist to reflect the more explicit automation. GitHub: *@jtenner*
- **cli: disable packaged windows wazero until Bun stops crashing on dlopen** Update the packaged-target mapping so `bun-windows-*` builds no longer stage or smoke-test the `wazero` addon through the compiled executable, because the standalone Windows Bun runtime is still crashing when it loads the native `.node` addon; keep the source-based Windows addon path intact and document that packaged Windows artifacts are currently `js`-only. GitHub: *@jtenner*
- **cli: keep windows run wrappers on the entry-file drive** Update the `as-harness run` wrapper generation so Windows no longer writes the temporary `entry.ts` beside the repository checkout when the requested test file lives on a different drive; the wrapper now uses the first entry file's directory as its temp base and explicitly rejects mixed-drive entry sets, which fixes packaged Windows runs that were emitting broken `import "./C:/..."` specifiers. GitHub: *@jtenner*
- **cli: keep the bundled harness lib virtual on windows** Update the compiler wrapper so Windows no longer materializes the bundled `node:assert` and `node:test` custom-lib aliases into a temporary directory on disk, which avoids `ENOTDIR` failures when the packaged CLI tries to create `node:assert` paths under the temp `--lib` root and instead lets AssemblyScript resolve the bundled harness library through the existing virtual file-system hooks. GitHub: *@jtenner*
- **wazero: stage windows node.lib as cgo-safe linker flags** Update the wazero addon build script so Windows no longer passes a bare absolute `node.lib` path through `CGO_LDFLAGS`; the script now stages the import library under `.cache/`, rewrites the directory to a cgo-safe path, and links it as `-L... -l:node.lib`, which avoids Go's invalid-flag rejection on hosted Windows builds. GitHub: *@jtenner*
- **wazero: download Node headers when hosted installs omit node_api.h** Update the wazero addon build script so it no longer assumes the active Node installation already includes `include/node/node_api.h`; the script now falls back to downloading and caching the matching Node headers archive from `process.release.headersUrl`, which restores hosted packaged-CLI builds on runners where `node.exe` is present but local headers are not. GitHub: *@jtenner*
- **wazero: guard float16 typed-array support by header capability** Move the generic typed-array byte-length switch in the Go N-API addon behind a small C helper that only references `napi_float16_array` when the installed Node headers define `NODE_API_HAS_FLOAT16_ARRAY`, which restores addon builds on runners that use older header sets while preserving `Float16Array` support where available. GitHub: *@jtenner*
- **assembly: move guest adapter sources off Windows-reserved node:* paths** Rename the on-disk AssemblyScript adapter source tree from `node:assert` and `node:test` filesystem paths to Windows-safe `node_assert`, `node_test`, and `node_test_lib` names, update the guest imports and assembly README to follow those safe paths, teach the bundled virtual-file generator to keep publishing the public `node:assert`, `node:assert/strict`, and `node:test` library aliases, add compile coverage for the alias map, and avoid Bun test discovery treating the safe `node_test` library entry as a test file. GitHub: *@jtenner*
- **cli: build wazero addons through node during packaging** Update the CLI packaging build so the local wazero addon step resolves and invokes a real Node executable instead of reusing Bun's `process.execPath`, which fixes hosted packaged-build failures where the addon build script would otherwise search for `node_api.h` relative to Bun rather than the configured Node installation. GitHub: *@jtenner*
- **docs: rewrite the markdown set around the public harness ABI** Refresh the tracked documentation set across the repo so the README family, planning docs, package docs, and adapter TODO pages all describe the same current architecture; add a dedicated `docs/003-2026-03-17-harness-abi.md` guide that documents the guest Wasm boundary and host-facing harness surface for third-party implementers; update the backlog to mark the completed workflow, feature-matrix, troubleshooting, wazero build-story, and protocol-note documentation items; and keep the project instructions compact while expanding the docs from implementation-first notes into a coherent “bring your own harness” reference set. GitHub: *@jtenner*
- **harness: share js and wazero host parity smoke coverage** Replace the duplicated `js` and `wazero` smoke assertions with a shared host-behavior suite that compiles common fixtures once per package cache, proves matching event decoding, `callI32`, `discover`, `run`, `start`, and trampoline behavior across both shipped hosts, adds a missing shared `callI32` error-path check, keeps the JS-only parallel-start and wazero-only CLI smoke cases package-local, and marks the explicit host-surface parity checklist items complete in `agent-todo.md`. GitHub: *@jtenner*
- **ci: add hosted release-target build and smoke workflows** Add canonical release-target metadata for the packaged CLI, expose local commands for listing and building just the intended release targets, add root helper scripts that emit the GitHub Actions matrix and verify one packaged CLI target end to end, then add CI and tag-driven release workflows that install Bun, Node, and Go on matching hosted runners, build the packaged executable, smoke-test it through `js` and `wazero`, and upload the resulting release artifacts while refreshing the packaging docs to reflect the new matrix flow and remaining proof gaps. GitHub: *@jtenner*
- **cli: fix compiled runner startup and bundled wazero loading** Update the compiler wrapper so the bundled strict-equality transform is passed to `assemblyscript/asc` as a direct transform object instead of a temp-file-only path, which clears the standalone Bun executable's compiler startup failure; then update the packaged `wazero` loader to use build-time-defined addon target and addon path constants so the compiled CLI can embed and resolve the single matching `.node` addon, prove the built executable through both `js` and `wazero`, mark the host-runtime bundling decision complete in `agent-todo.md`, and refresh the packaging docs to reflect the resolved blocker and the remaining cross-target release work. GitHub: *@jtenner*
- **cli: stage target-matched wazero addons for Bun builds** Update the CLI build and runtime packaging path so each compiled Bun target defines a single `WAZERO_TARGET`, loads only literal `.node` branches for that target, stages matching local `wazero` addons into `cli/n-api/` when available, bundles the JS harness through a static import for compiled executables, serves more of the AssemblyScript support files virtually from the compiler wrapper, keeps the generated virtual-file bundle Biome-formatted automatically, and refreshes the packaging docs while noting that the standalone compiled runner still has a separate `asc` startup failure to resolve. GitHub: *@jtenner*
- **cli: fix run help exportStart wording** Update the `as-harness run --help` text so the documented forced compiler defaults match the live runner behavior, specifically that the harness-backed run path forces `--exportStart __start` instead of the older empty export-start placeholder. GitHub: *@jtenner*
- **cli: add end-to-end wazero CLI smoke coverage** Update the CLI runner so harness resolution can stay lazy until after compilation, then extend `harness/wazero` smoke coverage with a real `as-harness run --harness wazero` subprocess check that proves the CLI can compile and execute a simple `node:test` entry through the native addon path. GitHub: *@jtenner*
- **cli: wire run flag parsing and harness selection** Extend the CLI parser so `as-harness run` accepts the documented AssemblyScript compile flags, forwards them into the compile wrapper, adds parser coverage for representative run options, renames the execution selector to `--harness`, and adds explicit failure behavior for unsupported harness names. GitHub: *@jtenner*
- **cli: add a first js-backed run path** Turn `as-harness run` into a real default-`js` execution flow by compiling discovered entry files through the bundled harness compiler wrapper, generating a temporary wrapper entrypoint that re-exports the guest harness exports and imports the discovered tests, instantiating the compiled module through `harness/js`, emitting stable process exit codes for success/test-failure/compile-failure/host-failure cases, adding basic pass/fail summary output, and covering the CLI path with a subprocess run smoke test. GitHub: *@jtenner*
- **docs: record v0.1.0 scope and diagnostics decisions** Update `agent-todo.md` with the accepted `v0.1.0` product scope, release-channel, host-selection, support-matrix, stack-trace, and deep-equality policy decisions; extend `docs/002-2026-03-13-strict-equality-machinery.md` with guest-owned stack-trace ABI and minimal shape-mismatch reporting notes; and align the root `README.md` with the GitHub-release and non-`musl` first-release direction. GitHub: *@jtenner*

## 2026-03-15

- **docs: rewrite the README set around the dual-host MVP** Replace the missing root README and refresh the package READMEs for `cli/`, `assembly/`, `harness/js`, `harness/wazero`, `cli/n-api`, and `scripts/` so they describe the current architecture, clearly separate implemented behavior from roadmap work, and document the intended MVP as shipping both the portable JS host path and the target-specific wazero Node-API addon path without overstating current CLI/runtime integration. GitHub: *@jtenner*
- **harness: ship standardized type definitions for js and wazero** Add a shared declaration source for the public harness API, wire both host packages to publish `index.d.ts` entrypoints with the same `Harness` and `HarnessStartResult` contracts, refine the callback and event typing to match the raw runtime payloads, and update the host READMEs to point at the shipped declaration surface. GitHub: *@jtenner*
- **js harness: parallelize start() branch discovery to match wazero** Update the shared JS `start()` orchestration so it no longer discovers top-level branches serially on the main thread; instead it now runs both branch discovery and branch execution through the same bounded worker-thread scheduling pattern used by the native `wazero` harness, adds focused smoke coverage for worker-owned branch discovery, and refreshes the JS host README wording to document the aligned behavior. GitHub: *@jtenner*
- **wazero: move start scheduling into the native Go addon** Replace the temporary JS-side `decorateHarness(...)` wrapper in `harness/wazero` with a native addon-owned `start()` method that performs branch discovery and execution scheduling inside Go using goroutines, resolves a real JS `Promise` with the formalized raw start result shape, simplifies the CommonJS wrapper, tightens both host smoke suites to assert promise semantics, and updates the host READMEs to document the stabilized result contract. GitHub: *@jtenner*
- **harness: add Promise-based start() orchestration to js and wazero** Add a shared host-side `start()` runner that performs full branch discovery, counts discovered tests, assigns top-level branches onto a worker-thread pool sized from host parallelism, and returns raw per-branch discovery and execution data; wire that surface into `harness/js` and a new JS wrapper for `harness/wazero`, expand both smoke suites to cover the new raw start results, and update the host READMEs to document the API. GitHub: *@jtenner*

## 2026-03-14

- **docs: trim completed checklist items from the backlog** Update `agent-todo.md` so it keeps only the remaining work items and release blockers, removing completed checklist entries and a few stale unchecked items that are already covered by the current implementation. GitHub: *@jtenner*
- **docs: add explicit publish blockers for shipping the runner** Update `agent-todo.md` with a dedicated publish-blockers section covering the remaining product-definition, CLI runner, dual-host (`js` and `wazero`) shipping, ABI, UX, and release-engineering work required to ship this project as an AssemblyScript test runner. GitHub: *@jtenner*
- **docs: align markdown with the current host and test layout** Update the project structure guide, assembly package README, both host READMEs, and the scripts README so they describe the live `js` and `wazero` host packages, the current package-local host smoke test entrypoints, and the root script/test workflow accurately after the new JavaScript host landed. GitHub: *@jtenner*
- **js host: add a pure Node WebAssembly harness** Add a new `harness/js` package that mirrors the current `wazero` host API in JavaScript, including immediate wasm validation, decoded `write_event` callbacks, host-managed trampoline support via `invoke_staged()`, staged `discover(nodeIndex)` and `run(nodeIndex)` execution, and matching smoke coverage against the shared AssemblyScript fixtures. GitHub: *@jtenner*
- **docs: close out the current node:test scope** Update the assembly package README, roadmap, `node:test` adapter TODO, and `agent-todo.md` so the current internal synchronous `node:test` declaration/discovery/execution core is treated as complete for the intended package scope, while deeper runner semantics such as fuller replay validation, todo suppression, richer callback failure modeling, snapshots, programmatic stream APIs, and worker-oriented execution controls are explicitly deferred unless that host-runner work is reopened later. GitHub: *@jtenner*
- **node:test: add root-to-target replay and immediate only filtering** Extend the shared `node:test` traversal runtime so targeted `run()` and staged `discover(nodeIndex)` replay from the root through ancestor callbacks on every attempt, reset replay-local child and hook state before rediscovery, and treat immediate-scope `only` metadata as active discovery and execution filtering for top-level `.only(...)` declarations and callback-scoped `t.runOnly(...)` nested subtests. Expand internal traversal coverage plus wazero smoke coverage for replay invalidation and filtered nested discovery/execution, regenerate the bundled virtual AssemblyScript sources, update the assembly package README, roadmap, `node:test` adapter TODO, and mark the corresponding replay items done in `agent-todo.md`. GitHub: *@jtenner*
- **node:test: add first error state and runOnly handling** Add a shared failure-state module so assertion failures record the active message pointer for `TestContext.error`, extend the execution/context layer with callback-scoped `t.runOnly(...)` support that marks nested `context.test(...)` declarations as `only`, add internal coverage for the new failure storage and nested-only behavior, regenerate bundled virtual AssemblyScript sources, and update the assembly docs to reflect the broader execution-oriented context surface. GitHub: *@jtenner*
- **cli: ignore the generated virtual-files barrel in Biome** Update [cli/biome.json](./cli/biome.json) to exclude the generated `as/.generated/virtual-files.barrel.ts` source from Biome checks so routine validation no longer stalls on a non-maintained generated file while keeping the rest of the CLI tree covered. GitHub: *@jtenner*
- **node:test: expose first execution-scoped attempt and passed metadata** Extend the shared execution-state layer so the node executor tracks the active attempt and whether the node callback has completed successfully, wire `TestContext.attempt` and `TestContext.passed` onto that state, add internal executor coverage for the lifecycle-hook view of those properties, regenerate bundled virtual AssemblyScript sources, and update the assembly docs to reflect that those two metadata getters are no longer hard-coded placeholders. GitHub: *@jtenner*
- **node:test: add first execution-scoped t.plan assertion counting** Extend the shared `node:test` runtime with attempt-local assertion-count tracking, wire `t.plan(count)` and the bound `t.assert.*` facade into that counter, fail node execution with a `FailMessage` when the planned count does not match the observed bound assertion calls, expand internal execution coverage plus wazero smoke coverage for both the matched and mismatched paths, regenerate bundled virtual AssemblyScript sources, and update the assembly docs to reflect that `t.plan(...)` now exists in first-pass form. GitHub: *@jtenner*
- **node:test: add host-observed diagnostic events** Extend the shared AssemblyScript event model with a `Diagnostic` payload that carries the active `NodeIndex` plus UTF-8 message text, expose `t.diagnostic(...)` on the early `node:test` context facade, wire the wazero host addon with decoded `onDiagnostic(...)` callbacks, expand the node:test smoke and compile coverage, regenerate bundled virtual AssemblyScript sources, and update the assembly and wazero docs to reflect the new context/event surface. GitHub: *@jtenner*
- **node:test: enforce skip and todo discovery branch semantics** Extend the shared AssemblyScript traversal helpers so discovery prunes descendants under skipped nodes while still allowing descendant discovery under todo nodes, expand internal traversal coverage and host-observed `node:test` smoke coverage for those branch semantics, regenerate bundled virtual AssemblyScript sources, and update the assembly docs to reflect the new discovery behavior. GitHub: *@jtenner*
- **node:test: add host-observed execution event smoke coverage** Extend the `node:test` smoke fixture with top-level lifecycle hooks, expand the wazero smoke to assert decoded `NodeStart`, `NodePass`, `CallbackStart`, `CallbackPass`, and `FailMessage` delivery and ordering during `run(nodeIndex)`, and regenerate bundled virtual AssemblyScript sources so the host-observed event contract is exercised end to end. GitHub: *@jtenner*
- **node:test: add staged child discovery by NodeIndex** Extend the shared AssemblyScript traversal helpers so `discover()` can rediscover immediate children under a staged `NodeIndex`, add a dedicated `discover(nodeIndex)` host method to the wazero harness, expand internal traversal coverage plus host-observed smoke coverage for top-level discovery, nested discovery, and interrupted discovery on trapping callbacks, regenerate bundled virtual AssemblyScript sources, and update the assembly and wazero docs to reflect that discovery interruption is now surfaced separately from execution outcomes. GitHub: *@jtenner*
- **node:test: add root discovery events through wazero** Extend the shared AssemblyScript traversal helpers and `assembly/assembly/exports.ts` with a first `discover()` export that emits `NodeFound` for already-registered top-level nodes without classifying outcomes, wire the wazero host `write_event` sink to decode event payloads and invoke the registered JS callbacks, expand the `node:test` smoke to assert decoded `NodeFound` delivery through `callI32("discover")`, regenerate bundled virtual AssemblyScript sources, and update the assembly and wazero docs plus checklist files to reflect that root discovery now exists while full-depth replay and interrupted-branch handling remain follow-up work. GitHub: *@jtenner*
- **node:test: add targeted NodeIndex run wiring through wazero** Add shared AssemblyScript traversal helpers for resolving a staged `NodeIndex`, export a first guest-side `run()` entrypoint from `assembly/assembly/exports.ts`, wire the wazero harness `run(nodeIndex)` path to call that export after writing the staged index buffer, add internal traversal coverage plus a host-observed `node:test` smoke fixture, regenerate bundled virtual AssemblyScript sources, and update the assembly and wazero docs plus checklist files to reflect that targeted node execution now exists before full discovery and replay support. GitHub: *@jtenner*
- **node:test: add the first internal runnable executor slice** Add `assembly/assembly/internal/executor.ts` with normal-node execution that emits `NodeStart` / `NodePass`, runs `beforeAll` / `beforeEach` hooks in root-to-leaf order and `afterEach` / `afterAll` hooks in leaf-to-root order, refactor callback invocation through a shared `Node.invokeCallback()` helper, add internal execution-order coverage, regenerate bundled virtual AssemblyScript sources, and update the assembly docs and checklist files to reflect that `node:test` is no longer declaration-only. GitHub: *@jtenner*
- **node:test: add declaration-time metadata getters and skip/todo methods** Extend the early `node:test` context layer with read-only metadata getters on `SuiteContext` and `TestContext`, add declaration-time `t.skip(...)` and `t.todo(...)` that retag the active node's declaration mode during callback discovery, expand internal and CLI compile coverage for those context behaviors, regenerate bundled virtual AssemblyScript sources, and update the assembly docs to reflect the broader declaration-time context surface. GitHub: *@jtenner*
- **node:test: add declaration-time TestContext methods and a partial t.assert facade** Extend the early `node:test` adapter with declaration-time `TestContext.test(...)`, `before(...)`, `after(...)`, `beforeEach(...)`, and `afterEach(...)`, add a first `t.assert` facade that forwards the currently supported synchronous assertion surface onto the existing `node:assert` bridge with strict `equal` / `deepEqual` semantics, add internal and CLI compile coverage for those context APIs, fix unsupported unmanaged reflected values so they stop reading managed runtime type ids, regenerate bundled virtual AssemblyScript sources, and update the assembly docs to reflect the broader but still declaration-only `node:test` context surface. GitHub: *@jtenner*
- **node:test: add the first declaration-registration adapter slice** Add the first package-style `node:test` lib entry point with `test` / `it` / `suite` / `describe`, modifier namespaces, top-level shorthand modifiers, and top-level hook registration, extend the shared AssemblyScript node runtime with durable declaration metadata, hook storage, explicit current-node control, and `NodeIndex` derivation, add internal declaration-graph coverage plus CLI compile coverage for `import "node:test"`, regenerate bundled virtual AssemblyScript sources, and update the assembly docs and checklist files to reflect that runnable execution and `t.assert` remain follow-up work. GitHub: *@jtenner*
- **docs: close out node:assert and hand off to node:test** Update the assembly package README, roadmap, `node:assert` adapter TODO, `node:test` adapter TODO, and `agent-todo.md` so the current synchronous `node:assert` bridge is treated as complete for the intended scope, stale checklist items that the existing bridge already satisfies are marked done, and the next adjacent work is explicitly framed as binding `t.assert` onto the completed package surface. GitHub: *@jtenner*
- **docs: defer the remaining node:assert surface from v1** Update the assembly package README, roadmap, and `node:assert` adapter TODO so the remaining unimplemented `node:assert` APIs are explicitly treated as deferred scope rather than open-ended near-term work, including legacy loose deep equality, regex-backed assertions, matcher-aware throw checking, and the `Assert` / `AssertionError` object-model APIs. GitHub: *@jtenner*
- **node:assert: add legacy equal and notEqual bridge helpers** Extend the shared AssemblyScript assertion bridge with a first-pass loose-equality helper for coercive `node:assert` comparisons, add dedicated legacy `equal(...)` and `notEqual(...)` adapter exports on plain `node:assert` plus the bundled package-style lib root, expand internal bridge tests, host-observed smoke coverage, and CLI compile coverage for the new package surface, regenerate bundled virtual AssemblyScript sources, and update the assembly docs to reflect that legacy `equal` / `notEqual` now exist while legacy deep equality remains pending. GitHub: *@jtenner*
- **docs: mark Promise-based assertions unsupported without Promise support** Update the assembly package docs, roadmap, and the `node:assert`, `node:test`, `jest`, `vitest`, and `jasmine` adapter TODOs to state explicitly that Promise-dependent assertion helpers and async polling APIs such as `rejects(...)`, `doesNotReject(...)`, `.resolves`, `.rejects`, and `waitFor(...)` are out of scope until AssemblyScript has Promise support strong enough to model those APIs coherently. GitHub: *@jtenner*
- **docs: mark mocking and call-tracking assertions unsupported without closures** Update the assembly package docs, roadmap, and the `node:test`, `jest`, `vitest`, and `jasmine` adapter TODOs to state explicitly that function mocking, spies, and call-tracking assertions such as `toBeCalled(...)` and `toHaveBeenCalledTimes(...)` are out of scope until AssemblyScript has closure support strong enough to model those APIs coherently. GitHub: *@jtenner*
- **node:assert: add the strict namespace alias** Expose `assert.strict` as a namespace alias of the existing `node:assert/strict` bridge entry point through both the source adapter and the bundled package-style lib root, add host smoke coverage and CLI compile coverage for `import { strict } from "node:assert"`, extend the internal assertion-bridge tests with direct `ifError(...)` coverage, refresh the assembly README and smoke-build logging to reflect the broader bridge surface, and regenerate bundled virtual AssemblyScript sources while leaving regex-backed assertion APIs blocked on separate runtime support. GitHub: *@jtenner*
- **node:assert: add default assert and ifError bridge exports** Extend the shared AssemblyScript assertion bridge with an `ifError(...)` primitive for nullish-only success checks, expose the callable/default `assert(value[, message])` alias plus `ifError(...)` through both `node:assert` entry points and the bundled package-style lib files, add smoke coverage for default-imported `assert(...)` and `ifError(...)`, add CLI compile coverage for `import assert from "node:assert"`, regenerate bundled virtual AssemblyScript sources, and update the assembly adapter docs to reflect the expanded synchronous bridge surface while leaving regex-backed assertions for later design work. GitHub: *@jtenner*
- **node:assert: add trap-backed throws and doesNotThrow bridge helpers** Extend the shared AssemblyScript assertion bridge with trampoline-backed `throws(...)` and `doesNotThrow(...)` helpers, expose those APIs through both `node:assert` entry points and the bundled package-style custom-lib files, expand the host-observed smoke modules to cover direct trap detection plus inner assertion failure-message emission across the trampoline boundary, adjust the test bootstrap/import stubs for the now-live imported runtime hooks, regenerate bundled virtual AssemblyScript sources, and update the assembly adapter docs to reflect the wider bridge surface while deferring matcher-aware `throws` / `doesNotThrow` semantics. GitHub: *@jtenner*
- **cli: materialize bundled package-style node:assert lib components** Add a dedicated bundled custom-lib tree with package-style `node:assert` and `node:assert/strict` entry files, update the compiler wrapper to rewrite symbolic harness library requests to one materialized temp `--lib` root while still auto-enabling the strict-equality transform, add CLI coverage that compiles a real `import "node:assert"` probe through the bundled lib root, regenerate bundled virtual AssemblyScript sources, and update the CLI and adapter docs to reflect the finalized custom-lib packaging model. GitHub: *@jtenner*
- **node:assert: add the first deepStrictEqual bridge slice** Add a shared AssemblyScript assertion bridge that lowers strict-structural mismatches into optional `FailMessage` emission plus `unreachable`, expose the first `deepStrictEqual(...)` entry points for `node:assert` and `node:assert/strict`, add internal and host-observed smoke coverage for pass and fail paths, regenerate bundled virtual AssemblyScript sources, and update the assembly docs and checklist files to reflect the new boundary while leaving raw custom `--lib` packaging follow-up work explicit. GitHub: *@jtenner*
- **strict-equality: tighten unmanaged policy and finish class reflection hooks** Align the strict-equality and reflected-diagnostics implementation with a safe-by-default unmanaged policy by skipping transform auto-instrumentation for `@unmanaged` classes, routing unmanaged defaults through only safe shared paths such as identity and arraylike comparison, adding explicit consumer hook opt-ins for custom unmanaged equality and reflected key/value extraction, finishing recursive class reflected-value construction through the shared hook contract, expanding transform and AssemblyScript coverage for unmanaged and class-hook behavior, regenerating bundled virtual AssemblyScript sources, and updating the checklist, design docs, package READMEs, and changelog to document the final contract. GitHub: *@jtenner*
- **strict-equality: add reflected Set and Map diagnostics support** Extend the shared AssemblyScript reflected-value runtime with dedicated `Set` and `Map` reflected kinds, typed map-entry modeling, and recursive collection handling through the active-reference tracker, expand AssemblyScript coverage for plain, nested, and self-referential reflected sets and maps, regenerate bundled virtual AssemblyScript sources, and update the checklist, docs, READMEs, and changelog so the remaining reflected-value gap is narrowed to managed-class reflection. GitHub: *@jtenner*
- **strict-equality: add reflected array and view diagnostics support** Extend the shared AssemblyScript reflected-value runtime with ordered array / `StaticArray` reflection, typed-array and `ArrayBufferView` / `DataView` byte reflection, and active-reference tracking for recursive collection reflection, expand AssemblyScript coverage for arrays, nested arrays, static arrays, typed arrays, and `DataView`, regenerate bundled virtual AssemblyScript sources, and update the checklist, docs, READMEs, and changelog so the remaining reflected-value work is narrowed to `Set`, `Map`, and managed-class reflection. GitHub: *@jtenner*
- **strict-equality: route nested Set and Map values through the dispatcher** Update the shared AssemblyScript strict-equality value dispatcher to send generic `Set<T>` and `Map<K, V>` references through the existing collection helpers instead of failing immediately, add AssemblyScript coverage for direct and nested dispatcher-driven set and map comparisons, regenerate bundled virtual AssemblyScript sources, and update the checklist, docs, READMEs, and changelog to reflect that the remaining major equality work is now on the reflected-value runtime and assertion bridge. GitHub: *@jtenner*
- **strict-equality: add first reflected-value runtime slice** Add a shared AssemblyScript reflected-value runtime module with the initial type model, primitive, string, and `ArrayBuffer` constructors, replace the generated reflected key/value helper stub with a live collector-backed implementation, add AssemblyScript coverage for the reflected-value model and collector behavior, regenerate bundled virtual AssemblyScript sources, and update the checklist, docs, READMEs, and changelog so the current boundary is explicit: primitive reflected values and the generated key/value collector are implemented while collection and generic managed-class reflected values remain pending. GitHub: *@jtenner*
- **strict-equality: add function-reference identity semantics** Add shared AssemblyScript strict-equality handling for function references by identity, route the generic value comparator through that runtime path instead of failing immediately on functions, expand AssemblyScript coverage for same-function, different-function, null-function, and generated-member-helper behavior, regenerate bundled virtual sources, and update the checklist, docs, READMEs, and changelog so the remaining runtime gap is narrowed to fully generic nested `Set` / `Map` dispatch plus reflected values. GitHub: *@jtenner*
- **strict-equality: add Set and Map helper routing** Add shared AssemblyScript strict-equality helpers for direct `Set` and `Map` comparison, introduce dedicated generated-member helpers for `Set`-typed and `Map`-typed fields and getters, teach the transform to classify those member types separately and emit the new helpers, expand AssemblyScript and transform coverage for collection equality and helper selection, regenerate bundled virtual sources, and update the checklist, docs, READMEs, and changelog to reflect that direct `Set` / `Map` comparison plus generated-member routing are now implemented while fully generic nested `Set` / `Map` dispatch and reflected values remain pending. GitHub: *@jtenner*
- **strict-equality: add typed-array and ArrayBufferView helper routing** Add bytewise strict-equality comparison for typed arrays, `ArrayBufferView`, and `DataView` in the shared AssemblyScript runtime, introduce a dedicated generated-member helper for view-typed fields and getters, teach the transform to classify typed-array and `DataView` members separately from generic value members, expand AssemblyScript and transform coverage for view equality and helper selection, regenerate bundled virtual sources, and update the checklist, docs, READMEs, and changelog to reflect that typed-array comparison is now implemented while sets, maps, and reflected values remain pending. GitHub: *@jtenner*
- **strict-equality: add ordered array and StaticArray comparison** Add shared AssemblyScript strict-equality runtime helpers for ordered `Array<T>` and `StaticArray<T>` comparison, route generic value comparison through those branches before the managed-class fallback, reuse the active-pair and proven-pair machinery so nested recursive class elements terminate cleanly, expand AssemblyScript coverage for primitive arrays, static arrays, nested buffers, and arrays of recursive class graphs, regenerate bundled virtual sources, and update the checklist, docs, READMEs, and changelog to reflect that arrays and arraylikes are now implemented while typed arrays, sets, maps, and function references remain pending. GitHub: *@jtenner*
- **strict-equality: add specialized ArrayBuffer comparison** Add bytewise `ArrayBuffer` comparison to the shared AssemblyScript strict-equality runtime, route `ArrayBuffer` values through that path from the generic value comparator, introduce a dedicated generated-hook helper for `ArrayBuffer`-typed members, teach the transform to classify `ArrayBuffer` fields and getters separately from generic value members, expand AssemblyScript and transform coverage for buffer equality and helper selection, regenerate bundled virtual sources, and update the checklist, docs, READMEs, and changelog to reflect that `ArrayBuffer` comparison is now implemented while arrays, typed arrays, sets, and maps remain pending. GitHub: *@jtenner*
- **docs: sync strict-equality plan with managed-class recursion support** Update the strict-equality machinery design doc so the generated-hook contract and Phase 3 status accurately reflect the newly implemented managed-class recursion path through transform-generated hooks and shared pair tracking. GitHub: *@jtenner*
- **strict-equality: route managed-class recursion through generated hooks** Add a dedicated managed-class member helper to the shared AssemblyScript strict-equality runtime, use the existing active-pair and proven-pair tracking to recurse safely through instrumented class graphs including cycles, teach the transform to classify known class-typed fields and getters separately from primitive and collection-placeholder members, emit the managed-class helper only for those members, expand AssemblyScript and transform fixtures to cover recursive class comparison and helper selection, regenerate bundled virtual sources, and update the checklist, docs, READMEs, and changelog to reflect that managed-class recursion is now implemented while collection-specialized comparison remains pending. GitHub: *@jtenner*
- **strict-equality: add recursive pair tracking for reference comparisons** Add shared AssemblyScript strict-equality runtime state for active reference pairs and proven reference matches, implement the first reusable `Match` / `Fail` / `Defer` pair-comparison helper for recursive graphs, expand AssemblyScript coverage to prove deferred re-entry, pair-cache reuse, and cleanup on failure, regenerate bundled virtual sources, and update the checklist, docs, README, and changelog to reflect that cycle-tracking is now implemented. GitHub: *@jtenner*
- **strict-equality: add runtime-type guards and paired member comparison** Update the strict-equality transform so generated class hooks guard `other` by same-instance identity and runtime type before comparing members, change per-member delegation to pass both the local and casted peer member values into the shared runtime helper, add the first runtime type-id and string-comparison helpers on the AssemblyScript side, expand transform and AssemblyScript tests to cover the new contract, regenerate bundled virtual sources, and refresh the planning docs, READMEs, changelog, and checklist to match the new implementation state. GitHub: *@jtenner*
- **strict-equality: add member-helper delegation and primitive runtime checks** Extend the strict-equality transform so generated class hooks delegate each selected field and getter through shared runtime helper calls, add transform fixtures that assert the emitted helper-call AST shape, implement the first shared AssemblyScript equality helpers for primitive fast paths, nullable-reference identity, and `NaN` normalization, regenerate bundled virtual sources, and update the planning docs, READMEs, changelog, and checklist to reflect the new runtime and transform state. GitHub: *@jtenner*
- **strict-equality: add member selection and inherited hook bodies** Add transform-side participating-member selection for instance fields and getters, define the first member-hash representation for inherited-member suppression work, extend the generated hook scaffolds so derived classes delegate into `super`, add direct CLI compiler-wrapper tests for strict-equality transform auto-enablement, expand transform fixtures for fields, getters, generics, and inheritance, and update the planning docs and checklist. GitHub: *@jtenner*
- **strict-equality: add first contract and transform implementation pass** Define the strict-equality Phase 1 contracts in the design docs, add the initial AssemblyScript strict-equality runtime contract scaffold and tests, auto-enable the bundled transform for `node:assert` library builds, implement the first real class-instrumenting transform pass with namespace recursion and placeholder hook injection, add transform fixture coverage, and update the relevant README and checklist files. GitHub: *@jtenner*

## 2026-03-13

- **cli: bundle and materialize transform assets for asc** Add `cli/transform/` scaffolding plus a loadable no-op AssemblyScript transform entrypoint, extend the virtual-file generator to bundle precompiled JS transform assets alongside the virtual AssemblyScript sources, teach the compiler wrapper to hoist bundled transform files to a temp directory and rewrite `--transform` paths before invoking `asc`, update the strict-equality planning doc and checklist, and document the transform hoisting behavior in the CLI README. GitHub: *@jtenner*
- **assembly: add a host-managed trap trampoline for callback assertions** Add a staged `() => void` guest trampoline plus the `invoke_staged()` imported ABI, wire wazero to re-enter the guest through `invoke()` and map trap vs normal return into `0` or `1`, add smoke coverage for both paths, update the assembly checklist to mark the new minimal assertion primitive as defined, and document the boundary in the assembly and wazero READMEs. GitHub: *@jtenner*
- **docs: inventory node:test and node:assert adapter surfaces** Expand `assembly/assembly/node:test/TODO.md` and `assembly/assembly/node:assert/TODO.md` with Node `v25.8.x` API inventories, required function and type breakdowns, and an implementation-oriented design for the first `node:test` adapter pass so the Wasm runtime work can proceed from a concrete surface definition. GitHub: *@jtenner*
- **docs: require checklist updates before commit** Update `AGENTS.md` to require marking completed `agent-todo.md` items before commit when the corresponding work is actually done. GitHub: *@jtenner*
- **docs: audit assembly task checklist against implemented runtime work** Update `agent-todo.md` to mark only the AssemblyScript checklist items that are explicitly completed in the current codebase, leaving the remaining tasks unchecked instead of auto-completing inferred work. GitHub: *@jtenner*
- **assembly: add a lazy-discovery Node runtime model** Add `assembly/assembly/internal/node.ts` with a `Node` class that stores core node metadata, parent linkage, the discovery callback, lazily discovered children via `getChildren()`, and a global `rootNode` / `currentNode` pair for parent-aware child creation directly through `node.createChild(...)`, add internal AssemblyScript coverage for the new behavior, regenerate the bundled virtual sources, and update assembly package docs. GitHub: *@jtenner*
- **harness: instantiate wazero modules and copy NodeIndex input on run** Update the Go-based `harness/wazero` addon so `run(nodeIndex)` instantiates the compiled module, calls a new AssemblyScript `allocateNodeIndexBuffer(length)` export, copies the requested `NodeIndex` into guest memory, returns `true` or `false` for execution success, and documents the new host/guest ABI step. GitHub: *@jtenner*
- **assembly: clarify the exports placeholder entrypoint** Remove the internal test import from `assembly/assembly/exports.ts`, document that the file is intentionally empty until a CLI-driven export path exists, and regenerate the bundled virtual source copy. GitHub: *@jtenner*
- **cli: bundle virtual assembly sources into the compiler wrapper** Add generated bundled AssemblyScript source text for `~/.as-harness`, regenerate it during CLI builds, and update the compiler wrapper to serve virtual files from the bundled module instead of scanning the repo at runtime. GitHub: *@jtenner*
- **assembly: add exports entrypoint for future test modules** Add `assembly/assembly/exports.ts` as a dedicated Wasm-export entrypoint and update `assembly/README.md` to distinguish it from the current internal test barrel workflow. GitHub: *@jtenner*
- **docs: add assembly package readme** Add `assembly/README.md` describing the package layout, current internal runtime work, test workflow, and planned framework adapter structure. GitHub: *@jtenner*
- **docs: expand scripts readme coverage** Update `scripts/README.md` to document the root validation script, the AssemblyScript test runner, and the generated-Wasm bootstrap script. GitHub: *@jtenner*
- **assembly: add internal event serialization and test bootstrap** Add the `assembly/assembly/internal` runtime event modules, internal serializer tests, and a root Bun test script that compiles and bootstraps the AssemblyScript test entrypoint. GitHub: *@jtenner*
- **docs: add assembly adapter entry-point skeletons** Add initial `assembly/assembly/*` library-entry folders plus per-adapter `TODO.md` files for the first set of framework and assertion adapters. GitHub: *@jtenner*
- **docs: correct assembly adapter checklist structure** Update `agent-todo.md` so the API layer work is expressed as framework-specific `assembly/assembly/*` `--lib` entry points instead of a single generic front-facing API layer. GitHub: *@jtenner*
- **docs: add assembly buildout task checklist** Add `agent-todo.md` with grouped implementation tasks for the `assembly/` Wasm runtime based on `docs/001-2026-03-13-primary-buildout.md`. GitHub: *@jtenner*
- **docs: clarify import-backed assembly declarations** Update `docs/001-2026-03-13-primary-buildout.md` to separate front-facing test APIs from the internal `test` / `describe` / `skip` / `todo` declaration primitives that call through WebAssembly imports. GitHub: *@jtenner*
- **tooling: add Biome-based validation workflow** Add Biome to `cli/`, initialize its config, add root `scripts/validate.ts` plus `bun validate`, and align `AGENTS.md` with the new validation command. GitHub: *@jtenner*
- **docs: add compact AGENTS guide and workflow rules** Add a concise project map plus validation, commit, and changelog rules in `AGENTS.md`. GitHub: `@jtenner`

### Added

- Added `harness/` as a placeholder for the AssemblyScript harness work.
- Added a new standalone `cli/` Bun package for the AssemblyScript harness work.
- Added Bun package metadata, scripts, and executable wiring for
  `@as-harness/cli`.
- Added a multi-target standalone build script in `cli/build.ts`.
- Added `cli/as/compile.ts` as the programmatic AssemblyScript compiler wrapper.
- Added `cli/runtime/` with shared runtime typing plus `js`, `wasmtime`, and
  `wazero` runtime stubs.
- Added `cli/n-api/` as a placeholder for bundled native modules.
- Added a repo-level changelog.

### Changed

- Expanded `cli/index.ts` from the Bun starter into a real CLI scaffold.
- Added top-level help and version handling.
- Added `list` command entry discovery for explicit file paths and glob-based
  scanning.
- Added scaffolded `run` command behavior.
- Added `run --help` with command-specific documentation.
- Added default test-entry discovery patterns:
  `**/*.{test,spec}.ts` and `test/**/*.ts`.
- Added `--glob` / `-g` and `--ignore` / `-i` support for entry discovery.
- Added `--coverage` and `--coverage-format` as parsed placeholders.
- Reworked the compiler wrapper to call AssemblyScript programmatically via
  `assemblyscript/asc` instead of shelling out.
- Reduced the exposed compiler option surface to the subset the harness intends
  to own.
- Forced harness-level compiler defaults for target, output path, debug mode,
  start-function export, and color handling.
- Changed compiler artifact handling from `Blob` output to structured artifacts
  with `path`, `contents`, and `contentType`.
- Implemented AssemblyScript `readFile` and `listFiles` hooks against the local
  filesystem.
- Introduced runtime-based compiler argument mutation through dependency
  injection.
- Replaced the short package README with detailed package-level documentation.

### Verified

- Verified Bun source execution for the CLI entrypoint.
- Verified `bun build --compile` output for every supported compile target.
- Verified `clean`, `build`, and `build:list-targets`.
- Verified `tsc --noEmit` for the `cli/` package.
