# `mocha` Adapter TODO

- [ ] Define the `mocha` declaration surface to expose from this `--lib` entry point.
- [ ] Map `it`, `describe`, skip/todo variants, and lifecycle APIs onto the shared internal declarations.
- [ ] Keep adapter-specific overloads and naming inside this folder.
- [ ] Add initial source files and a minimal traversal fixture.
