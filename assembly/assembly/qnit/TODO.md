# `qnit` Adapter TODO

Status: planned, not in `v0.1.0`.

First slice:

- map QUnit-style grouping and lifecycle to shared runtime
- adapter-specific naming only
- add one traversal fixture

Constraint: keep in shared runtime and shared ABI.
